const testeControllers = {

    create: (request, response) => {

    

        response.render('index', {
            title: 'express'
        });
    },
}

module.export = testeControllers;


// aqui esta quebrado